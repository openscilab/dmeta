- id: clear-metadata
  name: dmeta — clear metadata from Office, image, and audio files
  description: |
    Recursively strip metadata from Microsoft Office, image, and audio files
    in-place before each commit.
  entry: dmeta --clear-all --inplace
  language: python
  additional_dependencies: [dmeta]
  pass_filenames: false
